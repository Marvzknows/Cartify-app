
// export const sliceArrayByFour = (arr) => {

//     const chunks = [];
//   let currentChunk = [];

//   arr.forEach((element, index) => {
//     currentChunk.push(element);

//     // Check if current chunk is complete or if we've reached the end of the array
//     if (currentChunk.length === 4 || index === arr.length - 1) {
//       chunks.push(currentChunk);
//       currentChunk = []; // Reset current chunk
//     }
//   });

//   return chunks;
// }